---
- name: 'Ansible Playbook for Nvidia Cluster'
  hosts: nvidia_nodes
  become: true
  roles:
    - nfs
    #- common
    - dns
    #- ldap
    #- kerberos
    #- ssh
  environment:
    DEBIAN_FRONTEND: noninteractive
