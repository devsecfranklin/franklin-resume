---
# run this to be sure ntp is running at the end of setup ntpctl -sa

- name: "ntp | Configure /etc/{{ntp_obsd_configuration_file }}"
  ansible.builtin.template:
    src: "{{ntp_obsd_configuration_file }}"
    dest: /etc/ntpd.conf
    mode: 0644
  tags:
    - ntp