# csr

* View the Contents of the CSR in Plain Text `openssl req -in your_csr_file.csr -noout -text`
* Verify the CSR's Signature `openssl req -in your_csr_file.csr -noout -verify`
* Verify the CSR Matches the Private Key
