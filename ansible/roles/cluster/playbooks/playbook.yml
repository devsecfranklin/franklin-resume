---
- name: 'Ansible Playbook: lab.bitsmasher.net'
  hosts: raspi_servers nvidia_nodes raspi_nodes 
  become: true
  vars:
    ansible_python_interpreter: "/usr/bin/python3"
  roles:
    - dns
    - dhcp
    - nfs
    - ntp
    - ldap
    - kerberos
    - security